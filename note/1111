set http_proxy=http://127.0.0.1:7897     
set https_proxy=http://127.0.0.1:7897


curl -I http://httpbin.org/ip

curl -I --socks5 127.0.0.1:7897 http://httpbin.org/ip




我需要写一个冒险岛自动打怪的python程序，通过键盘操作，现在帮我打一个框架，通过这个框架我可以很轻易地搭建出一个自动打怪逻辑，这个框架不需要获取游戏的各种状态，只需要对键盘按键进行操作，帮我封装合适的函数实现自动吃血吃蓝，自动上buff，自动走动放技能，各种时间的间隔通过自定义的参数控制